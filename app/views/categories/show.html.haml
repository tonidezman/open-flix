%section.genre
  %header.clearfix
    %h1= @category.name
  %article.row
    - cache @category.videos do
      - @category.videos.each do |video|
        .video.col-md-2
          = link_to video do
            %img(src="#{video.small_cover_url}")

